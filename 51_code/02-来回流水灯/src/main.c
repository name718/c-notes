#include <STC89C5xRC.H>
#include <INTRINS.H>

typedef unsigned char u8;
typedef unsigned int u16;

void Delayms(u16 count);

// ??????
void main()
{
    // ???????
    u8 temp = 0x01;
    // ?????????????????
    bit is_left = 1;

    // ?????
    while (1)
    {
        // ?? P0 ????????
        P0 = ~temp;

        // ?????????
        // is_left ? temp <<= 1 : temp >>= 1;
        if (is_left)
        {
            temp <<= 1;
        }
        else
        {
            temp >>= 1;
        }

        // ???????????????????????
        if (temp == 0x80)
        {
            is_left = 0;
        }

        // ???????????????????????
        if (temp == 0x01)
        {
            is_left = 1;
        }

        // ???100ms
        Delayms(100);
    }
}

// ??????????????????????
void Delayms(u16 count)
{
    u8 data i, j;
    while (count)
    {
        _nop_();
        i = 2;
        j = 199;
        do
        {
            while (--j)
                ;
        } while (--i);
        count--;
    }
}
