#include "Com_Util.h"

// 主函数 外部中断
void main()
{
    // 1. 启动外部中断0 -------------------------
    // 1.1 打开中断总开关
    EA = 1;
    // 1.2 打开INT0开关
    EX0 = 1;

    // 2. 设置外部中断0的触发方式为下降沿触发 -----
    IT0 = 1;

    // 3. 设置外部中断0的优先级，设置位最高 -------
    // 3.1 设置低位为1
    PX0 = 1;
    // 3.2 设置高位为1
    IPH |= 0x01;

    // 死循环
    while (1)
        ;
}

// 4. 定义外部中断0的中断服务程序  --------------------
void INT0_Handler() interrupt 0
{
    // LED1亮灭转换
    P00 = ~P00;
}
