# 隐式转换
# 基本数据类型
# float 和 double 区别
# 常见占位符 %d.....
# 类型转换
# 进制表示
# printf 和 scanf 的返回值分别有什么意义
# i++ 和 ++i 的区别
# if,while 里面的判断必须是 boolean 类型的吗？
# switch 和 if-else 的区别
# case 后面为什么要求常量表达式？
# do...while 和 while 的区别？
# break 与 continue 的区别？
# 声明和定义的区别？
# C 语言参数传递本质是什么？
# 作用域和生命周期
# 局部变量和全局变量分别存哪里？
# auto、static、extern、register
# static 在不同场景下的作用
# extern 的本质？
# 宏
# .h 和 .c 的职责分工
# 组名表示什么？
# 二维数组在内存中如何存？
# #include <stdio.h>和#include "stdio.h"
# 数组、字符串常见方法
# 为什么 C 字符串要以 '\0' 结束？
# strlen 返回的是不是数组总大小？
# 为什么 strcpy 有风险？
# strcmp